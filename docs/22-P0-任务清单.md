# 📋 P0 任务清单（细化版）

> 更新日期: 2026-06-17
> 维护者: moqun188 / sxbwork
> 目标: 在 1-2 周内完成紧急修复与最小可用落地

---

## 📊 任务统计

| 状态 | 数量 |
|------|------|
| 待分配 | 0 |
| 进行中 | 0 |
| 已完成 | 0 |
| **总计** | **5** |

---

## 🔧 任务详情

### P0-001: 修复 statsManager NaN 问题

**任务 ID**: P0-001  
**优先级**: 🔴 紧急  
**预估工时**: 1h  
**负责人**: **sxbwork**  
**状态**: ❌ 待开始  

**问题描述**:
`statsManager.js` 中的 `getStats()` 方法计算平均维度分数时，由于 `dimensions[k]` 是对象而非数字，导致计算结果为 NaN。

**问题代码**:
```javascript
// 文件: pipeline/statsManager.js, 第 97 行
avgDimensions[k] = Math.round(
  inspections.reduce((s, r) => s + (r.dimensions[k] || 0), 0)  
  // 问题：r.dimensions[k] 是对象 { score, reason }，不是数字！
  / total * 10
) / 10;
```

**修复方案**:
```javascript
avgDimensions[k] = Math.round(
  inspections.reduce((s, r) => s + (r.dimensions[k]?.score || 0), 0)  
  / total * 10
) / 10;
```

**相关文件**:
- [pipeline/statsManager.js](file:///d:/tracecode/pipeline/statsManager.js#L97)

**验收标准**:
1. ✅ 代码修改符合修复方案
2. ✅ 运行 `npm start` 无报错
3. ✅ 访问 `/api/stats` 接口返回正确的 `avgDimensions`（非 NaN）
4. ✅ 提交 PR 并通过 moqun188 Review

---

### P0-002: 清理重复 API 调用

**任务 ID**: P0-002  
**优先级**: 🔴 紧急  
**预估工时**: 2h  
**负责人**: **moqun188**  
**状态**: ❌ 待开始  

**问题描述**:
当前质检流程中，ASR、说话人分离、情绪分析三个模块各自独立调用小米 MiMo API，每次都需要重新编码音频文件，导致：
- Credits 消耗 3 倍
- 质检耗时过长（15-30 秒）
- 重复的文件 IO

**优化目标**:
合并为单次多模态 API 调用，使用 `mimo-v2.5-pro` 一次性产出所有结果。

**相关文件**:
- [pipeline/index.js](file:///d:/tracecode/pipeline/index.js)
- [pipeline/asr.js](file:///d:/tracecode/pipeline/asr.js)
- [pipeline/diarization.js](file:///d:/tracecode/pipeline/diarization.js)
- [pipeline/emotion.js](file:///d:/tracecode/pipeline/emotion.js)

**验收标准**:
1. ✅ 质检流程仅调用 1 次 MiMo API（音频）+ 1 次（文本总结）
2. ✅ 质检耗时从 15-30 秒降低到 3-8 秒
3. ✅ 输出格式与原有格式兼容（utterances, emotion, summary）
4. ✅ 前端进度条显示正常

---

### P0-003: 修复 uploads 目录清理

**任务 ID**: P0-003  
**优先级**: 🟠 重要  
**预估工时**: 1h  
**负责人**: **sxbwork**  
**状态**: ❌ 待开始  

**问题描述**:
上传的 `.m4a`、`.mp3`、`.converted.mp3` 文件永久堆积在 `uploads/` 目录，占用磁盘空间。

**实现方案**:
```javascript
// server.js - 质检成功后清理
if (result.success) {
  fs.unlinkSync(req.file.path);  // 清理原始文件
  if (preprocResult.convertedPath && preprocResult.convertedPath !== req.file.path) {
    fs.unlinkSync(preprocResult.convertedPath);  // 清理转换后文件
  }
}
```

**相关文件**:
- [server.js](file:///d:/tracecode/server.js#L66)

**验收标准**:
1. ✅ 质检成功后，`uploads/` 目录中无残留文件
2. ✅ 质检失败后，文件保留用于排查
3. ✅ 提交 PR 并通过 moqun188 Review

---

### P0-004: 日志轮转与清理

**任务 ID**: P0-004  
**优先级**: 🟠 重要  
**预估工时**: 2h  
**负责人**: **moqun188**  
**状态**: ❌ 待开始  

**问题描述**:
- 所有模块写进一个文件，长期运行可能数 MB
- 无自动清理 / 轮转策略
- 无按会话 ID 分组，难以追踪某次质检的完整流水

**设计要求**:
1. **文件大小限制**：单文件 10MB 自动轮转
2. **会话 ID**：每次质检生成唯一 `sessionId`
3. **自动清理**：保留最近 7 天日志

**相关文件**:
- [pipeline/logger.js](file:///d:/tracecode/pipeline/logger.js)

**验收标准**:
1. ✅ 单个日志文件超过 10MB 时自动创建新文件
2. ✅ 日志格式包含 `sessionId`
3. ✅ 7 天前的日志文件自动删除

---

### P0-005: 文档更新与新人指引

**任务 ID**: P0-005  
**优先级**: 🟡 一般  
**预估工时**: 1h  
**负责人**: **moqun188**  
**状态**: ❌ 待开始  

**验收标准**:
1. ✅ 新人能在 5 分钟内阅读完指南
2. ✅ 指南包含完整的环境搭建步骤
3. ✅ 指南链接到其他详细文档

---

## 📅 Sprint 计划（按敏捷 Scrum 流程）

### Sprint 1 (2026-06-17 ~ 2026-06-23)

| 日期 | 任务 | 负责人 | 故事点 | 状态 |
|------|------|--------|--------|------|
| 周一-周二 | P0-001 修复 NaN | sxbwork | 2 | 🔲 |
| 周一-周三 | P0-002 清理重复 API | moqun188 | 8 | 🔲 |
| 周二-周三 | P0-003 uploads 清理 | sxbwork | 3 | 🔲 |
| 周三-周四 | P0-004 日志轮转 | moqun188 | 3 | 🔲 |
| 周五 | P0-005 文档更新 | moqun188 | 2 | 🔲 |
| 周五 | 集成测试 + Code Review | 全员 | - | 🔲 |

> 详见 [docs/05-敏捷开发流程.md](./05-敏捷开发流程.md)

---

## 📝 任务更新记录

| 日期 | 任务 ID | 更新内容 |
|------|---------|---------|
| 2026-06-17 | ALL | 初始创建任务清单 |
| 2026-06-17 | P0-001~005 | 分配负责人（moqun188 / sxbwork） |
