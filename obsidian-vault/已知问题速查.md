# 🐛 已知问题速查表

> 最后更新: 2026-06-17

---

## 已确认 Bug

### 🔴 高优先级

| ID | 描述 | 位置 | 状态 |
|----|------|------|------|
| BUG-001 | statsManager.avgDimensions 计算结果为 NaN | statsManager.js:97 | ❌ 未修复 |
| BUG-005 | 客服/客户角色标注反转 | diarization.js:229 | ✅ 已修复 (v0.1.2) |

**BUG-001 详情**:
```javascript
// 问题代码
avgDimensions[k] = Math.round(
  inspections.reduce((s, r) => s + (r.dimensions[k] || 0), 0)  
  / total * 10
) / 10;

// 修复方案
avgDimensions[k] = Math.round(
  inspections.reduce((s, r) => s + (r.dimensions[k]?.score || 0), 0)  
  / total * 10
) / 10;
```

**BUG-005 详情** (已修复 v0.1.2):
```javascript
// 问题代码 v0.1 - 硬编码 speaker_0 = agent
role: speaker === "speaker_0" ? "agent" : "customer",

// v0.1.1 修复 - 但仍以第一个说话人默认为客服（电话场景中接电话的是客户）
// v0.1.2 彻底修复 - 双说话人关键词得分比较
// 策略: 分别计算每个说话人的客服词和客户词得分
// 客服词得分高 → agent，客户词得分高 → customer
// 兜底: "您"的频率 → 默认第一个说话人为 customer（接电话方）
```

---

## 已知限制

| ID | 描述 | 规避方案 |
|----|------|----------|
| LIM-001 | qualityAnalyzer 仅支持退货场景评分 | 使用 summarizer 结果辅助评分 |
| LIM-002 | 日志文件无限增长 | 定期手动清理 logs/ 目录 |
| LIM-003 | 上传文件不自动清理 | 定期手动清理 uploads/ 目录 |
| LIM-004 | ASR 时间戳基于估算 | 等 API 返回真实时长 |

---

## API 调用成本

| 模块 | 当前调用次数 | 优化后 | 节省 |
|------|-------------|--------|------|
| ASR | 1 次 | 1 次 | 0% |
| 说话人分离 | 1 次 | 0 次 | 100% |
| 情绪分析 | 1 次 | 0 次 | 100% |
| 通话总结 | 1 次 | 0 次 | 100% |
| **总计** | **4 次** | **1 次** | **75%** |

---

## 关键配置检查清单

- [ ] MIMO_API_KEY 已配置（tp- 开头）
- [ ] MIMO_BASE_URL 指向正确集群
- [ ] ENABLE_REAL_ASR=true
- [ ] ENABLE_REAL_DIARIZATION=true
- [ ] ENABLE_REAL_EMOTION=true
- [ ] ENABLE_REAL_SUMMARY=true
- [ ] @ffmpeg-installer/ffmpeg 已安装

---

## 日志关键字

| 关键字 | 含义 |
|--------|------|
| `[ASR] 使用模拟模式` | 未启用真实 ASR |
| `[ASR] 回退到模拟模式` | API 调用失败 |
| `命中缓存` | MD5 缓存命中（优化后） |
| `命中模拟` | Key 校验失败 |

---

*用于快速定位和解决问题*
